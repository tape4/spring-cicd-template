global:
    scrape_interval: 15s

scrape_configs:
    - job_name: prometheus
      metrics_path: "/actuator/prometheus"
      static_configs:
          - targets:
                [
                    "host.docker.internal:${SPRING_APP_PORT_1}",
                    "host.docker.internal:${SPRING_APP_PORT_2}",
                ]
